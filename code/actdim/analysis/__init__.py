"""What the article says about a trained network that no training run establishes.

The closed-form representation of appendix M, the reference values the measured order
parameter is read against, and the proof that three of appendix O's polynomials admit no
decomposition the architecture can represent. None of it takes a gradient step, and none
of it writes a file.
"""
from __future__ import annotations
